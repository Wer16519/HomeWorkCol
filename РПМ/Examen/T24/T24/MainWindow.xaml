<Window x:Class="T24.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:T24"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="lstBooks" Margin="0,0,5,0" SelectionChanged="LstBooks_SelectionChanged" DisplayMemberPath="DisplayInfo"/>

        <StackPanel Grid.Column="1">
            <TextBox x:Name="txtAuthor" Margin="0,5" Text="Автор"/>
            <TextBox x:Name="txtTitle" Margin="0,5" Text="Название"/>
            <TextBox x:Name="txtGenre" Margin="0,5" Text="Жанр"/>

            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="Добавить" Click="BtnAdd_Click" Width="60" Margin="0,0,5,0"/>
                <Button Content="Изменить" Click="BtnUpdate_Click" Width="60" Margin="0,0,5,0"/>
                <Button Content="Удалить" Click="BtnDelete_Click" Width="60"/>
            </StackPanel>

            <TextBox x:Name="txtSearch" Margin="0,10,0,5" TextChanged="TxtSearch_TextChanged"/>
            <ListBox x:Name="lstSearch" Height="100" DisplayMemberPath="DisplayInfo"/>
        </StackPanel>
    </Grid>
</Window>