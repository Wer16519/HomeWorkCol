<Window x:Class="t21.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Form1" Height="400" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Slider x:Name="trackBar" Grid.Row="0" Minimum="1" Maximum="20" Value="1"
                TickFrequency="1" IsSnapToTickEnabled="True"
                ValueChanged="TrackBar_ValueChanged" Margin="0,10"/>

        <ComboBox x:Name="comboBox" Grid.Row="1" Width="150" HorizontalAlignment="Left"
                  Margin="0,10" SelectionChanged="ComboBox_SelectionChanged"/>

        <GroupBox x:Name="groupBox" Header="groupBox1" Grid.Row="2" Margin="170,10,0,0">
            <StackPanel x:Name="rbPanel" Margin="10"/>
        </GroupBox>
    </Grid>
</Window>